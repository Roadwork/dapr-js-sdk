export default interface IActor {
    // getId(): any;
    // saveData(): void;
    // getData();
    // registerReminder();
    // unregisterReminder();
    // registerTimer();
    // unregisterTimer();
    // onActivation(): void;
    // onDeactivation(): void;
}